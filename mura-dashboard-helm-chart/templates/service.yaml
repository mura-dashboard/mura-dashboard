apiVersion: v1
kind: Service
metadata:
  name: {{ include "mura-dashboard.fullname" . }}
  labels:
    {{- include "mura-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mura-dashboard.selectorLabels" . | nindent 4 }}
